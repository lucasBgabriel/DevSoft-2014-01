// This anonymous function will be executed after the page is loaded.
$(function() {


  // Hide all newly created tokens and show welcome div.
  $('.getStarted').hide();
  //$('.getStarted').show();

  // Start animation.
 // $('.getStarted span').eq(0).fadeIn(200, showNextToken);

});